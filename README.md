# bored

A Flutter app that leverages Riverpod for state management to present random activities fetched from an API or filtered activities sourced from dummy data (previously retrieved from an API) based on user selection.
